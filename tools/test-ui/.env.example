# Target used by the Vite dev server to proxy requests.
# In Docker Compose: use the service name (api:8080).
# When running Vite locally outside Docker: use localhost.
VITE_API_TARGET=http://localhost:8080
